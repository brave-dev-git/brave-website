---
import Layout from "../layouts/Layout.astro";
import Header from "../components/Header.astro";
import { site } from "../config/site";
import { getCollection } from "astro:content";

const title =
  "Brave Security – cybersäkerhet, riskhantering och säkra AI-lösningar";
const description =
  "Brave Security hjälper ledning och IT att förstå, prioritera och hantera risker – från cybersäkerhet och informationssäkerhet till säkerhetskultur och säkra AI-lösningar.";
  
const latestPosts = (await getCollection("insikter"))
  .filter((p) => !p.data.draft)
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, 3);
---


<Layout title={title} description={description} lang="sv">
  <div id="top"></div>
  <Header variant="overlay" />

  <main class="container">
    <!-- HERO -->
    <section class="section hero">
      <div class="section-surface hero-surface">
        <!-- Ambient bakgrund kvar -->
        <img
          class="hero-bg"
          src="/starting-page.png"
          alt=""
          aria-hidden="true"
          decoding="async"
          loading="eager"
        />

    <div class="hero-grid">
      <!-- FULLBREDDS-BILD -->
      <div class="hero-media hero-media--wide hero-media--flush" aria-hidden="true">
        <img
          src="/starting-page.png"
          alt="Abstrakt ljusvirvel i lila/blå toner"
          loading="eager"
          decoding="async"
          width="1600"
          height="900"
        />
      </div>
          <!-- VÄNSTER: TEXT -->
          <div class="hero-content">
            <h1 class="h1">No risk no reward.</h1>

            <p class="lead">
              Brave Security är en svensk säkerhetspartner inom cybersäkerhet, riskhantering och
              säkerhetskultur samt innovativa lösningar för modern säkerhet. Vi hjälper verksamheter
              att förstå läget, prioritera rätt och genomföra åtgärder som hanterar risk och bygger 
              säkerhetsförmåga på riktigt.
            </p>

            <div class="cta-row">
              <a class="btn-primary" href="/#kontakt">Boka samtal</a>
              <a class="btn-secondary" href="/#tjanster">Se tjänster</a>
            </div>

            <p class="micro">Snabbt första steg: maila eller ring – du når oss direkt.</p>
          </div>


          <!-- Höger: kort -->
          <div class="hero-side">
            <div class="card">
              <h2 class="h2">Vanliga utmaningar</h2>
              <ul class="bullets">
                <li>Otydlig lägesbild och svårt att prioritera</li>
                <li>Svårt att hantera många nya regelverk </li>
                <li>AI införs snabbare än styrning och kontroll</li>
              </ul>
              <a class="link" href="/utmaningar">Se fler utmaningar →</a>
            </div>

            <div class="card">
              <h2 class="h2">Så jobbar vi</h2>
              <ul class="bullets">
                <li>Anpassat och strukturerat</li>
                <li>Beslutsunderlag för ledning och styrelse</li>
                <li>Praktiska åtgärder som går att driva över tid</li>
              </ul>

              <p class="micro" style="margin-top: 0.6rem;">
                Säkerhet över tid: ett flexibelt partnerskap där ni väljer delar efter behov.
              </p>

              <a class="link" href="/sakerhetspartnerskap">Säkerhetspartnerskap →</a>
            </div>
          </div>
        </div>
      </div>
    </section>


    <!-- TJÄNSTER -->
    <section id="tjanster" class="section">
      <div class="section-surface">
        <div class="section-head">
          <h2 class="h2xl">Tjänster</h2>
          <p class="muted">
            Tre områden där vi oftast skapar effekt snabbt – och där AI-frågan ofta hänger ihop med helheten.
          </p>
        </div>

      <div class="grid3">
        <article class="card">
          <h3 class="h3">Riskhantering</h3>
          <p class="p">
            Struktur för att identifiera, värdera och prioritera risker – med tydliga åtgärder och ansvar.
          </p>
          <a class="link" href="/tjanster/riskhantering">Prata risk →</a>
        </article>

        <article class="card">
          <h3 class="h3">Cybersäkerhet & informationssäkerhet</h3>
          <p class="p">
            Från nuläge och gap till säkerhetsarkitektur, kontroll och genomförande – anpassat till din verklighet.
          </p>
          <a class="link" href="/tjanster/cybersakerhet">Stäm av läget →</a>
        </article>

        <article class="card">
          <h3 class="h3">Säkerhetskultur & övning</h3>
          <p class="p">
            Beteenden, förmåga och beredskap: övningar som tränar beslut, roller och kommunikation när det gäller.
          </p>
          <a class="link" href="/tjanster/sakerhetskultur-och-ovning">Planera övning →</a>
        </article>
      </div>
      </div>
    </section>

    <!-- UTMANINGAR -->
    <section id="utmaningar" class="section">
      <div class="section-surface">
        <div class="section-head">
          <h2 class="h2xl">Era utmaningar</h2>
          <p class="muted">
            Säkerhet börjar sällan med en produkt – den börjar med en fråga. Här är några vi ofta får.
          </p>
        </div>

      <div class="grid2">
        <article class="card">
          <h3 class="h3">Upptäckt & lägesbild</h3>
          <ul class="bullets">
            <li>Kommer vi märka om någon tar sig in?</li>
            <li>Har vi koll på våra mest kritiska tillgångar?</li>
          </ul>
        </article>

        <article class="card">
          <h3 class="h3">Prioritering</h3>
          <ul class="bullets">
            <li>Vilka åtgärder ger faktisk riskreduktion och stödjer vår affär?</li>
            <li>Vad kan vänta med utan att vi tar onödig risk?</li>
          </ul>
        </article>
      </div>
      
      <div class="cta-row">
        <a class="btn-secondary" href="/utmaningar">Alla utmaningar</a>
        <a class="btn-primary" href="/#kontakt">Boka samtal</a>
      </div>
      </div>
    </section>

    <!-- AI -->
    <section id="ai" class="section" aria-label="AI och säkerhet">
     <div class="section-surface">
      <header class="section-head">
      <h2 class="section-title">AI &amp; säkerhet</h2>
      <p class="section-desc">
        AI införs ofta snabbare än styrning och kontroll. Vi hjälper er få effekt snabbt — utan att tappa kontroll på data,
        integritet, beslut och ansvar.
      </p>
    </header>

    <div class="grid2">
      <article class="card">
        <h3 class="card-title">Vanliga risker</h3>
        <ul class="bullets">
          <li>Dataexponering via prompts, loggar och integrationer.</li>
          <li>Otydligt ägarskap och ansvar mellan IT, verksamhet och leverantör.</li>
          <li>Otillräcklig spårbarhet: vad användes, av vem och när?</li>
        </ul>
        <div class="cta-row">
          <a class="btn-secondary" href="/ai-sakerhet">Läs mer →</a>
        </div>
      </article>

      <article class="card">
        <h3 class="card-title">Snabbstart (2–4 veckor)</h3>
        <ul class="bullets">
          <li>Kartlägg use cases, dataflöden, integrationer och roller.</li>
          <li>Prioritera topprisker och åtgärder som ger faktisk riskreduktion.</li>
          <li>Sätt minimikrav (guardrails) för data, åtkomst och leverantör.</li>
        </ul>
        <div class="cta-row">
          <a class="btn-primary" href="/#kontakt">Boka samtal</a>
        </div>
      </article>
    </div>
    </div>
  </section>

  <!-- OM OSS -->

  <section id="om-oss" class="section" aria-label="Om Brave Security">
    <div class="section-surface">
    <header class="section-head">
      <h2 class="section-title">Om oss</h2>
      <p class="section-desc">
        Brave Security hjälper styrelse, ledning och IT att förstå läget, prioritera rätt och genomföra åtgärder som faktiskt minskar risk.
        Rakt, strukturerat och utan fluff.
      </p>
    </header>


    <div class="grid2">
      <article class="card">
        <h3 class="card-title">Så jobbar vi</h3>
        <ul class="bullets">
          <li>Beslutsunderlag som går att använda direkt.</li>
          <li>Praktiska åtgärder som går att driva i vardagen.</li>
          <li>Tydlig prioritering: vad ger effekt nu vs senare.</li>
        </ul>
        <div class="cta-row">
          <a class="btn-secondary" href="/om-oss">Mer om oss →</a>
        </div>
      </article>

      <article class="card">
        <h3 class="card-title">När vi ofta kommer in</h3>
        <ul class="bullets">
          <li>Otydlig lägesbild och svårt att prioritera.</li>
          <li>Stora initiativ men liten riskreduktion.</li>
          <li>AI införs snabbare än styrning och kontroll.</li>
        </ul>
        <div class="cta-row">
          <a class="btn-primary" href="/#kontakt">Prata med oss</a>
          <a class="btn-secondary" href="/utmaningar">Se utmaningar</a>
        </div>
      </article>
    </div>
      </div>
  </section>

    <!-- INSIKTER -->
    <section id="insikter" class="section" aria-label="Insikter">
      <div class="section-surface">
        <header class="section-head">
          <h2 class="h2xl">Insikter</h2>
          <p class="muted">Korta perspektiv och konkreta lärdomar inom cybersäkerhet, risk och AI.</p>
        </header>

        <div class="grid3">
          {latestPosts.map((post) => (
            <a class="card insights-post" href={`/insikter/${post.slug}/`}>
              <div class="insights-meta">
                <time datetime={post.data.date.toISOString()}>
                  {post.data.date.toLocaleDateString("sv-SE")}
                </time>

                {post.data.tags?.length ? <span class="insights-dot">•</span> : null}
                {post.data.tags?.slice(0, 2).map((t) => (
                  <span class="insights-tag">{t}</span>
                ))}
              </div>

              <h3 class="h2" style="margin-top: 0.6rem;">{post.data.title}</h3>
              <p class="p" style="margin-top: 0.5rem;">{post.data.description}</p>
              <div class="insights-more">Läs →</div>
            </a>
          ))}
        </div>

        <div class="cta-row" style="margin-top: 1rem;">
          <a class="btn-secondary" href="/insikter">Läs alla insikter</a>
        </div>
      </div>
    </section>


    <!-- KONTAKT -->
    <section id="kontakt" class="section">
      <div class="section-surface">
        <div class="section-head">
          <h2 class="h2xl">Kontakt</h2>
          <p class="muted">Välj det som passar. Du når oss direkt.</p>
        </div>

        <div class="grid3">
          <a class="card link-card" href={`mailto:${site.email}?subject=Kontakt%20-%20Brave%20Security`}>
            <div class="contact-k">E-post</div>
            <div class="contact-v">{site.email}</div>
            <div class="contact-hint">Maila oss →</div>
          </a>

          <a class="card link-card" href={`tel:${site.phoneTel}`}>
            <div class="contact-k">Telefon</div>
            <div class="contact-v">{site.phoneDisplay}</div>
            <div class="contact-hint">Ring →</div>
          </a>

          <a class="card link-card" href={site.linkedinCompany} target="_blank" rel="noopener noreferrer">
            <div class="contact-k">LinkedIn</div>
            <div class="contact-v">Brave Security</div>
            <div class="contact-hint">Följ oss →</div>
          </a>
        </div>

        <div class="foot">
          © {new Date().getFullYear()} {site.legalName}
        </div>
      </div>
    </section>

  </main>
</Layout>

<style>
  
.hero {
  padding: 0;
}

/* Behåll section-surface look, men ge den "hero-känsla" */
.hero-surface {
  position: relative;
  overflow: hidden;
  padding: 2.2rem;
}

/* Ambient bakgrundsbild (subtil) */
.hero-bg {
  position: absolute;
  inset: -140px -220px;
  width: calc(100% + 440px);
  height: calc(100% + 280px);
  object-fit: cover;
  opacity: 0.22;
  filter: saturate(1.05);
  pointer-events: none;
  z-index: 0;
}

/* En enda tint ovanpå ambient (viktig: bara EN) */
.hero-surface::after {
  content: "";
  position: absolute;
  inset: 0;
  background: rgba(2, 6, 23, 0.72);
  z-index: 0;
}

/* Layout */
.hero-grid {
  position: relative;
  z-index: 1;
  display: grid;
  gap: 1.4rem;
  grid-template-columns: 1.2fr 0.8fr;
  grid-template-areas:
    "media media"
    "content side";
}

.hero-media--wide { grid-area: media; }
.hero-content { grid-area: content; }
.hero-side { grid-area: side; }

/* Bildramen */
.hero-media--wide {
  border-radius: 20px;
  overflow: hidden;
  border: 1px solid rgba(148,163,184,0.18);
  background: rgba(2,6,23,0.25);
  height: clamp(220px, 26vw, 360px);
  margin: 0 0 0.6rem 0; /* liten luft under bilden */
}

.hero-media--wide img {
  display: block;
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: 50% 50%;
}

/* Högerkort */
.hero-side {
  display: grid;
  gap: 1rem;
}

.hero + .section {
  border-top: 0;
}

@media (max-width: 980px) {
  .hero-surface { padding: 1.6rem; }
  .hero-grid {
    grid-template-columns: 1fr;
    grid-template-areas:
      "media"
      "content"
      "side";
  }
}


</style>
